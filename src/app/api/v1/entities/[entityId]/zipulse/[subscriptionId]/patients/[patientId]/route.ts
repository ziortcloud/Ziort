// MOVED — Patient management is under ZiCare
import { gone } from '@/ziorbitcore/api/response'
export const GET   = () => gone('Patients moved to /zicare/ routes')
export const PATCH = () => gone('Patients moved to /zicare/ routes')
