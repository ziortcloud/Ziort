// MOVED — Doctor management is under ZiCare
import { gone } from '@/ziorbitcore/api/response'
export const GET  = () => gone('Doctors moved to /zicare/ routes')
export const POST = () => gone('Doctors moved to /zicare/ routes')
